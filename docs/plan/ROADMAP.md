# Season 1 implementation roadmap

P0 orientation, 2026-09-08. Inspected baseline: `3e2174f` on `main`. Read `00-README.md` through `07-CODEX-PROMPTS.md`, then `PRODUCT.md` and `TECHNICAL.md`, and checked the relevant implementation. References below describe that baseline; line numbers may move during implementation.

## How to use this roadmap

- **Prompt numbers and titles come from [07-CODEX-PROMPTS.md](07-CODEX-PROMPTS.md)**. Numbered priority lists in other documents are not interchangeable with prompt IDs.
- P0 changes documentation and example configuration only. All P1–P22 rows are planned, not completed or visually accepted.
- Execute prompts in source order after resolving their listed conflicts. The owner explicitly pulled asset URL/upload tooling forward from P18; this does not complete P18. Current owner policy is in [DECISIONS.md](DECISIONS.md).
- **High** risk: authority, accounting, privacy, payments, synchronization, compatibility, or launch. **Medium** risk: bounded presentation and tooling changes. Risk is not an effort estimate.
- Paths are repository-relative; directories identify groups of expected edits. Paths marked **new** are proposed additions. Every row includes relevant `TECHNICAL.md` updates when behavior changes.
- Unit tests belong beside their modules as `*.test.ts`/`*.test.tsx`; browser scenarios belong in `tests/e2e/`; every new RPC gets pgTAP in `supabase/tests/database/`, including grants/RLS and relevant row-lock/concurrency invariants. `pnpm verify` does not run pgTAP or Playwright.
- Keep `pnpm verify` green after each implementation prompt. Add tests for every new pure function. Do not add dependencies unless the executing prompt explicitly names them; do not add paid services.

## P1–P22

| Prompt | Title | Expected files to touch | Risk | Tests to add |
|---|---|---|---|---|
| P1 | Remove the ghost ground strip | `src/components/scene/PixiScene.tsx`; `src/content/countries/phase2-factory.ts`, `phase3-factory.ts` and affected preload declarations | Medium | Tbilisi regression with a documented deterministic pre-fix pixel signature; no ground texture in the v3 scene graph; preload exclusions without breaking legacy packs. |
| P2 | Ground truth: stage metadata, bottom-anchored panorama, correct scale | `src/lib/content/schema.ts`; **new** `src/lib/world/stage-layout.ts`; `src/lib/traveler/actor-layout.ts`; `src/components/scene/PixiScene.tsx`; `src/components/traveler/ProductCharacterStage3D.tsx`; `src/app/preview/[packId]/page.tsx`; **new** calibration component; Tbilisi/Tashkent packs | Medium | Defaults keep all 16 packs valid; stage-layout at 320×568, 390×844, 1440×900, 2560×1080; feet within 2 px of ground; desktop height constraint; zone transitions; preview access and JSON-copy behavior. |
| P3 | Make him part of the painting | `src/lib/characters/actor.ts`; `src/components/traveler/ProductCharacterStage3D.tsx`, `CharacterStage3D.tsx`, `CharacterReview.tsx`; `src/components/scene/PixiScene.tsx`, `SceneStage.tsx`; **new** shared visual-grade contract/helper | Medium | Toon-map and alpha-cutout preservation; skinned outline alignment; sponsor patch retained; shadow coordinates; shared grade and low-tier behavior; disposal on reload. |
| P4 | Distance-based route and day goals | **New** forward migration; `src/lib/content/schema.ts`, `contracts.ts`; `src/lib/bootstrap/server.ts`; heartbeat route; `src/lib/world/route-clock.ts`, `types.ts`; traveler motion/presentation clocks; Pixi scene; HUD; `scripts/process-phase2-art.mjs`; Tbilisi/Tashkent derived assets | High | Route boundaries and evening loop; invalid inputs; two-track monotonic clock and 60-second cap; action-pause semantics; pgTAP distance accrual, day boundaries, permissions and rollback compatibility; goal labels and transitions. |
| P5 | Pace: more watchers, faster walk | Forward migration/RPC; `src/lib/config/server.ts`; `src/lib/contracts.ts`; bootstrap/heartbeat; `src/lib/characters/product-timeline.ts`; presentation/motion clocks; `src/components/hud/LiveStatus.tsx` | High | Pace table 1/2/4/8/16/40 plus zero; accrual across joins, departures and expiry; duplicate-tab deduplication; gait/scroll agreement; two contexts observe ×2; stale labels. |
| P6 | The waiting state and the first watcher | Forward migration/RPC; bootstrap/heartbeat contracts; `src/lib/characters/product-timeline.ts`; motion clock; `src/components/hud/IntroHeadline.tsx`, `WalkingRuleStatus.tsx`; `src/components/journey/JourneyExperience.tsx` | High | Expiry-derived waiting timestamp; exactly one first-watcher award; 600-second boundary; simultaneous arrivals; authoritative wake transition; reconnect and rollover; card shown only to its recipient. |
| P7 | Watchers by country | Forward migration/RPC; heartbeat/bootstrap; **new** country-code/flag helpers and leaderboard UI; **new** `src/app/country/[cc]/page.tsx`; HUD | High | Header parsing and ZZ fallback; live-country distinct counts; country contribution deduplication; pgTAP aggregates/permissions; leaderboard ordering; forced trusted country header fixture. |
| P8 | Reactions and crowd-scheduled actions | Forward migrations/RPCs; **new** `src/app/api/reactions/route.ts`, `src/app/api/day-photos/route.ts`; validation/contracts; bootstrap/heartbeat; motion clock; **new** reaction UI and composed-frame capture helper | High | Thresholds and cooldowns; concurrent scheduling/collisions; 120-active-second deduplication; encounter deferral; reconnect/late-join determinism; upload authorization/type/size/deduplication; synchronized two-context actions. |
| P9 | Vote 2.0: neighbours, rollover, the Day-1 name vote | Schema and country packs; **new** candidate builder; vote/rollover migrations and RPCs; `src/lib/story-clock/schedule.ts`, `rollover.ts`; seed tooling; `src/content/countries/registry.ts`; vote route/UI | High | Neighbour selection, exhaustion/fallback and exclusion; review eligibility; tie/no-ballot behavior; name-vote transition; concurrent/idempotent rollover; one ballot per visitor; live results and countdown. |
| P10 | Local time of day and real weather | Forward weather-cache RPC/migration; **new** weather fetch/cache and grade helpers; bootstrap/heartbeat; Pixi/character stages; HUD; art derivation | Medium | Local-hour/DST and grade interpolation; WMO mapping; ten-minute cache and single refresh claim; stale/provider failure behavior; pgTAP cache write permissions; mocked weather and reduced-motion effects. |
| P11 | Animation retarget integration, gaze, waiting behaviours | `src/lib/characters/manifest.ts`, `actor.ts`, `timeline.ts`, `product-timeline.ts`, `props.ts`; content state schema; motion clock; both character stages/review UI; owner-supplied `public/characters/v3/` assets when available | High | Separate animation binding; alias/missing-clip fallbacks; deterministic seeking/waiting; gaze limits; prop cleanup; state-label agreement; every review state loads without errors using v2. |
| P12 | Landing, HUD rewrite, share cards and OG images | `src/components/journey/`, `hud/`, `vote/`; `src/app/globals.css`, `layout.tsx`, `page.tsx`; **new** `src/app/api/og/` routes and signed-token/share helpers; identity/bootstrap private state | High | HUD at 320/390/1440 widths; first-visit persistence; signed token expiry/tampering; confirmed/extrapolated labels; PNG responses/cache policy; native-share/clipboard fallback and personalized-card isolation. |
| P13 | Day outcomes, the recap page and the post kit | Outcome/rollover migration/RPC; **new** `src/app/day/[n]/page.tsx`, `src/app/api/admin/postkit/[n]/route.ts`; recap OG/render/storage and template helpers; protected post-kit UI | High | Outcome finalization and retry idempotency; accounting through day end; permanent sponsor attribution; template escaping; private admin access; seeded completed recap and stored image. |
| P14 | The journey map | **New** `public/map/world.svg`, map/projection component/helper, `src/app/map/page.tsx`; journey details integration | Medium | Projection boundaries; visited/current/candidate markers and links; outcome colors; reduced-motion pulse; three visited days/two candidates; SVG size budget. |
| P15 | Sponsor pricing engine and the sponsors page | Pricing/inventory migrations/RPCs; `src/lib/payments/`; checkout/webhook routes; sponsor form; **new** `src/app/sponsors/page.tsx`, `src/app/sponsor/[publicId]/report/page.tsx`; actor bottle and café placement; protected creative preview | High | Clamp, founding price and rounding; D+1…D+7 eligibility and concurrent reservation; locked purchase price; amount/currency mismatch; approval/refund gates; premium creative and report access. |
| P16 | Passport, streaks, season sheet | `src/components/archive/PassportArchive.tsx`; archive page; contribution/streak query RPCs; bootstrap; **new** `src/app/season/1/page.tsx`, season OG route | High | Thirty-second collection threshold; streak gaps and season boundaries; distinct visitor contribution; outcome stamp colors; private bootstrap fields; mixed-outcome season sheet. |
| P17 | Living world | Pixi scene and product character stage; **new** deterministic ambient schedules; pack defaults/ambient assets; runtime milestone flag migration/RPC | Medium | Deterministic schedules and variants; time/weather/reaction inputs; quality limits; pgTAP one-time 100-watcher flag; background wave-back; accelerated runtime errors, frame budget and disposal. |
| P18 | Performance, cost protection and deletions | Character optimize/load tooling; **new** asset URL helper and `scripts/upload-assets.mjs`; legacy renderer/assets/tests; schema/registry/preloads; **new** `/api/me` route; bootstrap/identity and heartbeat/TTL handling; `scripts/load/phase3-load.ts`; results docs | High | GLB decoder/clip compatibility and size; no deleted references; all retained packs validate; same-origin/CDN asset resolution and CORS; no personalized public cache or Set-Cookie; adaptive TTL with independent 60-second cap; 1,000-viewer reaction load gate. |
| P19 | Pack generator CLI | **New** pack scaffold/build scripts and `docs/plan/content-banned-words.txt`; `package.json`; schema/content validation; country factories/registry; Sofia source/derived assets | Medium | CLI validation/scaffolding without unwanted overwrites; strict allowed text slots and keyword checks; panorama derivation/edge blend/palette; budgets; generated pack registration and all-pack validation. |
| P20 | Locals' corrections loop | Corrections migration and submit/moderate/read RPCs; **new** `src/app/api/corrections/route.ts`, `src/app/api/admin/corrections/route.ts`; private moderation UI; correction form; country/footer integration | High | Enum/280-character bounds; Origin and three/hour limit; pgTAP grants/RLS; unauthorized moderation denied; no public body echo; accepted distinct contributor count and idempotent moderation. |
| P21 | Production launch configuration | `src/lib/config/phase2-policy.ts`, `server.ts`; launch seed/forward migration; `vercel.json`; cron/prewarm and health routes; **new** `docs/runbooks/launch-day.md`; calibrated Tashkent v5 registration | High | Environment/launch-time gate matrix; no prelaunch accrual; deterministic seed and retry-safe rollover; approved provider/asset readiness checks; failure health responses; `pnpm verify:phase3`. |
| P22 | "Buy him a ticket" (post-launch, week 2) | Ticket/inventory migrations/RPCs; sponsor checkout/approval/refund paths; rollover/candidate selection; map, vote and HUD contracts/UI | High | Horizon/day/price validation; concurrent day locks; approval versus mere payment; refund/cancellation policy; displaced vote and sponsor compatibility; continuation from ticket country; fixture purchase/notice. |

## Confirmed baseline and intended replacements

These are expected changes, not evidence that the corresponding prompt is implemented:

- **P1–P3:** the contact strip still exists (`src/components/scene/PixiScene.tsx:98`, `:204`, `:363`), actor sizing is viewport-based (`src/lib/traveler/actor-layout.ts:2`), and the product uses ACES plus a shadow floor (`src/components/traveler/ProductCharacterStage3D.tsx:69`, `:90`). The diagnosis in TECHNICAL §8 is confirmed.
- **P4:** `routePositionAt` loops over active-second durations (`src/lib/world/route-clock.ts:9`). Runtime authority has active seconds and steps, not a stored distance (`supabase/migrations/202608310001_phase1_core.sql:108`).
- **P9/P21:** scheduling and tomorrow selection use the seven-day route (`src/lib/story-clock/schedule.ts:3`, `src/content/countries/registry.ts:46`). Season 1 and voted scheduling are new behavior.
- **P11:** the manifest still serves v2, with visual review pending (`src/lib/characters/manifest.ts:2`). Automated checks do not accept character anatomy or naturalness.

## Conflicts and prerequisites to resolve

P0 records these issues; it does not change the source prompts or silently choose unresolved product policies. Technical adaptations below preserve the stated house rules. Resolve each item before its affected prompt.

| ID / prompt | Plan versus evidence | Required resolution |
|---|---|---|
| C01 — P2 | P2 locates interactive calibration at an API route. `src/app/api/admin/preview/[packId]/route.ts:14` returns JSON; the visual page is `src/app/preview/[packId]/page.tsx:10`. | Put calibration controls in the protected visual page and retain the JSON API contract. Copy JSON only, with no server persistence, as P2 specifies; §02.10's “save” step is not permission to add persistence. |
| C02 — P4 | TECHNICAL describes wall-clock story beats, but motion schedules `atFraction × routeDuration` at `src/lib/traveler/motion-clock.ts:124`; bootstrap separately selects timestamp events at `src/lib/bootstrap/server.ts:183`. | Migrate both schedules coherently so dialogue, action and status use the same authoritative action. Explicitly handle departure at rollover and existing scheduled events. |
| C03 — P4–P5 | Proposed distance accrues throughout watched time. Current motion deliberately holds locomotion during actions (`src/lib/traveler/motion-clock.ts:189`). Database steps use 1.8/s (`src/lib/config/server.ts:18`); gait uses 0.6-second plants (`src/lib/traveler/motion-clock.ts:3`). | Decide and document server distance/step behavior during actions and speed changes. Preserve truthful labels and a deterministic footfall/world timeline; do not accrue fictitious walking distance while displaying a stationary action. |
| C04 — P5 | §04.4 says count before upsert “so the caller counts.” Existing code upserts at `supabase/migrations/202608310002_phase1_security_and_rpcs.sql:110` and counts afterward at `:139`. | Separate prior-interval accrual from the post-mutation count/pace. Account for lease expiry inside the interval; do not apply a new arrival's pace retroactively. Test zero viewers and multi-tab deduplication. |
| C05 — P6 | P6 asks for a three-second wake while the endpoint immediately returns `walking: activeViewers > 0` (`src/app/api/presence/heartbeat/route.ts:61`). Waiting cannot be stamped only when an absent viewer sends a request. The one-minute test example disagrees with `FIRST_WATCHER_GAP_SECONDS=600`. | Derive waiting start from authoritative inactivity/expiry; define a shared wake phase and award exactly once after the configured gap. Correct the acceptance scenario to distinguish being first from qualifying for the card. |
| C06 — P7 | Adding each session's visible delta to country totals conflicts with current visitor contribution deduplication (`supabase/migrations/202609010004_phase2_content_postcards.sql:123`, `:145`). | Define country attribution using deduplicated visitor contribution, including simultaneous tabs and country changes. Read trusted deployment metadata; local forced-header fixtures do not prove trust at the public edge. |
| C07 — P8 | Actions scheduled at +2 seconds may arrive too late through the existing heartbeat timer (`src/hooks/useJourneyPresence.ts:62`). A single next-action object also omits an action already in progress when a viewer joins. | Specify timely shared action delivery and enough authoritative schedule history for late joins, reloads and deferred encounters. A local button pulse alone is not synchronization evidence. |
| C08 — P8 | §04 requires reaction counts never decrease (`docs/plan/04-IMPLEMENTATION-CHANGES.md:147`), but resets buckets on a trigger (`:158`). Fractional active seconds also conflict with the proposed integer action timestamp and unique timestamp collisions. | Resolve cumulative versus remaining counts, timestamp precision and collision allocation under a lock before writing the RPC. Preserve 120-active-second deduplication and plant-grid alignment. |
| C09 — P9 | The schema has `culturalReview.status`, not `reviewStatus`, and lacks `creator_reviewed` (`src/lib/content/schema.ts:223`, `:292`). P9 needs it before P19's content tooling. | Assign creator-review schema/eligibility support to P9 as an explicit prerequisite. Preserve reviewer evidence and require real creator review; do not bulk relabel provisional packs as reviewed. |
| C10 — P9 | The neighbour-only promise conflicts with nearest-unvisited fallback in P9. There are 16 pack versions but only 14 distinct registered cities (`src/content/countries/registry.ts:19`), insufficient for 30 distinct unvisited country-days. Day 1 votes on a name without specifying who chooses Day 2. | Owner must resolve fallback/revisits/content buffer and Day-2 destination policy. Select by country identity rather than treating Tashkent versions as separate destinations; no fabricated neighbours. |
| C11 — P11 | P11 depends on an owner-supplied v3 GLB, while v2 is current (`src/lib/characters/manifest.ts:5`). `sit` already exists (`src/lib/content/schema.ts:44`); `rest` is not proof of an authored sit-down/sleep transition. | Keep v2 fallback and missing-clip badges. Add only absent states, and label actual fallback behavior honestly. Record new model/retargeted clips as an external prerequisite, not a completed coding deliverable. |
| C12 — P12 | “Once per visitor via existing cookie” cannot be implemented by reading that cookie in client code: `src/lib/identity/cookie.ts:21` sets HttpOnly. | Carry onboarding state through server-owned/private visitor handling. Keep the identity cookie HttpOnly and prevent visitor-specific state entering public OG/bootstrap caches. |
| C13 — P13/P20 | Post-kit and corrections admin access “like preview” would disappear in production: `src/lib/admin/preview-auth.ts:8` denies production. An API endpoint also does not automatically provide an HTML editing/copy UI. | Define protected production admin access separately from non-production pack previews. Specify the API/UI split without weakening preview isolation. |
| C14 — P15/P22 | Inventory slots require a country-day (`supabase/migrations/202609010005_phase2_sponsorship_payments.sql:5`), but country-days require city/pack fields (`supabase/migrations/202608310001_phase1_core.sql:19`). Future countries will not be known when slots open. | Design date-based future inventory or explicit provisional scheduling before selling D+1…D+7. Define ticket interaction with already sold slots and vote outcomes; preserve one active sponsor per slot. |
| C15 — P15 | Checkout already accepts a computed price (`src/lib/payments/checkout.ts:29`). Orders validate against the reserved sponsorship's price/currency (`src/lib/payments/webhook.ts:50`), while P15 mentions validating against the pricing row and flagging mismatches. | Preserve the immutable purchase snapshot. Decide the exact mismatch/quarantine workflow and approval block; later price changes must not invalidate or silently approve an existing purchase. |
| C16 — P18 | Deletion includes v1 still selected by character review (`src/lib/characters/manifest.ts:12`), plus layers/props still required by schema (`src/lib/content/schema.ts:190`). P17 needs foreground cutouts that a blanket P18 deletion would remove. | Remove/update consumers, defaults and tests before deleting assets. Inventory P17/P15 live props separately; keep current review functionality with valid candidates and remove only unused files. |
| C17 — P18 | Public bootstrap caching would include selected ballot and postcard/contribution data (`src/lib/bootstrap/server.ts:195`, `:259`) and response cookies (`src/app/api/bootstrap/route.ts:31`). | Complete the public/private split before adding public cache headers. Public responses must not contain visitor state or Set-Cookie. Preserve private admission control and label stale confirmed counts. |
| C18 — P18 | The presentation clock extrapolates to supplied lease expiry (`src/lib/traveler/presentation-clock.ts:27`). Adaptive leases can exceed 60 seconds. The client already honors a server-provided interval (`src/hooks/useJourneyPresence.ts:62`). | Keep lease lifetime and maximum extrapolation distinct. Reuse the current interval contract or explicitly migrate it; test prolonged disconnects with 70/90-second TTLs. |
| C19 — P18 | Pack URLs are root-relative and validated that way (`src/lib/content/schema.ts:207`, `:265`); a server-only ASSET_BASE_URL is not directly readable by browser code. | Preserve canonical pack paths and resolve URLs at the rendering/loading boundary using public configuration supplied safely from the server. Include cross-origin image/texture CORS needed by P8 capture. |
| C20 — P19 | The prompt scaffolds root `content/countries`, but registered modules live under `src/content/countries` (`src/content/countries/registry.ts:1`). The schema uses non-strict Zod objects (`src/lib/content/schema.ts:186`), so unknown fields are not currently rejected. | Generate into the actual source directory and register packs. Explicitly implement strict allowed-slot validation with backward-compatible defaults. No unapproved YAML dependency; choose a supported input approach when executing P19. |
| C21 — P19 | Five distinct Sofia scenes need five source paintings; the existing Phase 3 pipeline crops one master, as implemented in `scripts/process-phase3-art.mjs:19`. A build script cannot create absent paintings. | Record source artwork as an input dependency; do not claim that five crops fulfill five distinct zones. |
| C22 — P20 | P20 defines “Reviewed with help from N locals” as the count of accepted corrections (`docs/plan/07-CODEX-PROMPTS.md:637`); multiple corrections can come from one visitor. Anonymous visitor hashes also do not verify residency. | Count distinct accepted contributors and resolve whether to use “contributors” rather than claiming verified locals. Keep correction text private and moderation idempotent. |
| C23 — P21 | Production is always denied (`src/lib/config/phase2-policy.ts:5`), the current cron is 00:05 UTC (`vercel.json:7`), and Tashkent v5 is not registered (`src/content/countries/registry.ts:3`). | P21 must explicitly change the production policy and schedule, using the owner's rollover choice; create/register calibrated v5 beforehand. Setting PHASE2_ENABLED alone is insufficient. |
| C24 — all database prompts | §04 proposes one `0011_season1.sql`; P4–P22 add schemas incrementally. Existing migrations end at `supabase/migrations/202609040010_phase3_atomic_bootstrap.sql:1`. | Use ordered, timestamped, append-only migrations for each implemented increment; never edit an applied migration. Extend the current atomic bootstrap/admission path when introducing v5. |

## Cross-document gaps and launch prerequisites

- **Numbering:** `docs/plan/06-LAUNCH-AND-GROWTH.md:142` calls corrections P18; canonical corrections are P20 and performance is P18. Use §07 numbering throughout execution.
- **Pre-launch order:** §01 calls P1–P16 pre-launch work, while §06 requires P18 asset/cost protection before launch. Keep P21 dependent on P18; do not imply that finishing P16 is launch readiness.
- **Owner checkpoints:** name/domain, shortlist and 16:00 UTC rollover are now confirmed in [DECISIONS.md](DECISIONS.md). Actual launch remains TODO; 23 September 2026 at 16:00 UTC is a working target only.
- **Unassigned behavior:** creator-review support is needed in P9; P19 scaffolds notebook lines and personal postcard copy but no prompt clearly owns their unlock/render behavior. Record that feature gap for an owner-approved follow-up rather than claiming the entire §01 wishlist is covered by P1–P22.
- **Season scheduling:** name voting, destination selection, 30-day content availability, no-vote ties, replayed rollover and ticket overrides need one consistent schedule model. Existing `reconcile_phase2_state` updates pre-existing days rather than creating tomorrow (`supabase/migrations/202609010006_phase2_rollover_metrics_storage.sql:69`).
- **Asset/model ownership:** P11 now includes CC0 acquisition and Blender retarget repairs by Codex. Manual Mixamo account work goes to the owner only when needed; per-clip procedural fallback is allowed. P19 includes image generation when available, otherwise per-zone prompts for owner PNGs. Sofia/Phase-3 paintings are week-1 work, not launch blockers. Preserve licenses/credits and verify terms before acquiring assets.
- **Services and launch:** real provider/payout validation, a domain/asset host, physical-device performance, creator content review and comprehension testing require actual evidence. The bundle suggests paid hosting upgrades; house rules prohibit adding paid services. Record these as owner launch decisions and verify current service terms when that work is requested, rather than provisioning anything in P0.
- **Review-route preservation:** later deletion must preserve `/preview/characters` as a working test surface, consistent with the owner's earlier instruction. A deleted baseline must not remain a broken dropdown option.

## Owner resolutions after P0

The conflict table above retains its baseline evidence. These answers supersede its open owner-policy wording; the related implementation remains required:

| Affected work | Decision / acceptance additions |
|---|---|
| C09 / P9, P19 | Documented owner checklist review is sufficient; add eligibility support without automatically approving provisional packs. |
| C10 / P9 | Sequential Day-1 name/destination ballots, single chip, noon switch in the 16:00-to-16:00 journey day. Extra split has a one-hour/risk limit; otherwise name-only Day 1 and fixed Dushanbe Day 2. Test switch boundaries, retries, no overlapping ballots and both fallback paths. |
| C10 / P9, P14, P22 | Rolling ready content; non-neighbour transfers explicitly named train/flight and dashed on map. Preserve travel mode in server contracts, status and map. Test that no transfer appears as walking. |
| P9 candidates | Apply unordered blocked pairs AM–AZ, AM–TR, RS–XK, GR–TR and every pair involving IL/RU for Season 1. Test order symmetry, whole-ballot filtering, nearest ready replacement and exhaustion. No global country ban. Tashkent start; include KZ/GE/TR when possible. |
| C11 / P11 | Codex owns CC0 motion acquisition and Blender retargeting; owner supplies manual Mixamo FBX only if necessary. Preserve current per-clip fallback and missing-clip indicators. |
| C13 / P13, P20 | Separate signed HttpOnly admin session, 12-hour expiry; secret at least 48 characters, exchange limited to five/hour/IP, no secret logs. Return 404 to unauthenticated admin requests. Test expiry, tampering, rate limit, log redaction and 404 behavior. |
| C14 / P15, P22 | Approved pricing and ticket policy are in DECISIONS. Full refund on creative rejection; restore the normal destination vote. Test refund retry/idempotence, approval cutoff and vote restoration under the day lock. |
| C19 / P18 partial | Asset URL helper and upload tooling pulled forward now. Canonical pack paths remain local; a validated public origin is embedded at build time. Upload only public runtime trees; dry-run default, no deletes, private S3 credentials. Test URLs, upload preflight, signing and failures. Live R2/CORS verification waits for the owner's bucket. |
| C21 / P19 | Deliver pack:build and distinct per-zone prompts. Generate pictures if available; otherwise owner PNG inputs. Sofia/Phase-3 paintings are week-1 work, not launch blockers. |
| C22 / P20 | Use exactly “Improved with help from N contributors”; count distinct accepted contributors. |
| C23 / P21 | Rollover 16:00 UTC confirmed. Store/payout eligibility unchecked, no real $1 payment/refund rehearsal. Actual launch TODO until every gate passes; working target 23 September 2026, 16:00 UTC. |

This decision-recording increment does not implement P1–P22's other product changes. Payment eligibility, R2 provisioning, actual content review, device/comprehension gates and final launch confirmation remain owner/external work.

Verification for this increment, 2026-09-08: `pnpm verify` passed lint, typecheck,
39 unit-test files / 127 tests, and the production build. `pnpm assets:upload`
completed a dry run: 851 files, 78,992,133 bytes, zero uploads. Live R2 credentials,
uploads and CORS remain unverified. No additional visual tests, images or videos
were produced. Manual hosting checks and rollback are in the
[asset hosting runbook](../runbooks/asset-hosting.md).

## P0 acceptance and verification (historical)

- Only `docs/plan/ROADMAP.md`, `docs/plan/DECISIONS.md` and `.env.example` are intended changes. No product code, dependencies, database state, deployed configuration or assets change in P0.
- ROADMAP has exactly P1–P22, with canonical titles, expected paths, risk and tests. Its conflicts cite baseline file:line evidence and distinguish future replacement from an unresolved contradiction.
- DECISIONS records the five fixed decisions and four owner-choice categories; unselected values remain TODO. Environment defaults are examples, not evidence of feature implementation or owner approval.
- Baseline and post-edit `pnpm verify` both passed on 2026-09-08: lint, typecheck, 37 unit-test files / 103 tests, and production build (exit 0). A separate documentation check confirmed all 22 canonical prompt titles, twelve exact environment defaults with no duplicate keys, and valid file:line references. These checks do not establish pgTAP, browser, visual, payment or physical-device acceptance.
- `next build` regenerates tracked `next-env.d.ts` from `.next/dev/types` to `.next/types`. Restore only that generated delta to its pre-check contents before the scoped commit; do not include a product-file change in this documentation task.
- Manual review: compare all roadmap titles to §07, resolve the conflict register before affected prompts, fill owner TODOs when ready, and compare the twelve `.env.example` additions to §04.9. No visual tests, images or videos are required for P0.
