/**
 * The October 1 anniversary update shown in Journey once the fourteen days are complete.
 * Owner-edited: replace `body` with the real update (and set `postedOn`) when there is one.
 * Until then it says only what is true.
 */
export const ANNIVERSARY_UPDATE = {
  title: "Anniversary update",
  body: "The fourteen days are complete. My anniversary update will be posted here.",
  /** "YYYY-MM-DD" once a real update replaces the placeholder. */
  postedOn: null as string | null,
};
